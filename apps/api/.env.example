# Base de données locale de développement (PostgreSQL lancé via docker compose
# à la racine du monorepo). Copier ce fichier vers .env et adapter si besoin.
# Le vrai fichier .env est ignoré par Git (cf. .gitignore).
DATABASE_URL="postgresql://finance:PASSWORD@localhost:5432/finance_dev?schema=public"
